<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.sberbank.lesson13.task.aidl.client">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="ru.sberbank.lesson13.task.aidl.client.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name="ru.sberbank.lesson13.task.aidl.client.ExampleIntentService"
            android:enabled="true"/>
        <receiver
            android:name="ru.sberbank.lesson13.task.aidl.client.ExampleBroadcastReceiver"
            android:enabled="true"/>
    </application>

</manifest>